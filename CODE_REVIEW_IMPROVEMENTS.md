# 어드민 전반 코드 검토 및 개선 항목

검토일: 2025-02-09  
대상: ai_curatorhub_admin 프로젝트 전체

---

## 1. 이미 수정 완료된 항목

### 1.1 도구 상세 조회 – 다른 제품 클릭 시 하단 텍스트박스 미갱신
- **원인**: Streamlit 위젯이 동일한 `key`로 재사용되어, 선택이 바뀌어도 이전 값이 유지됨.
- **조치**: 상세 영역의 모든 위젯에 `selected_tool_id` 기반 동적 key 적용 (예: `key=f"detail_id_{_tk}"`).

### 1.2 다국어 관리 – 검색 버튼/한·영 검색
- **원인**: 검색 패널·액션 바·목록·상세가 `tab1` 밖에 있어 검색 UI가 탭과 분리되어 보였고, 검색 시 `id` 필드 중복으로 인도네시아어 필드가 문서 id와 충돌 가능.
- **조치**:
  - UI 텍스트 번역 탭 내부로 검색 패널·액션 바·목록·상세 편집 전부 이동.
  - 검색 필드 목록에서 `id` 중복 제거, `(t.get(field) or "")` 로 빈 값 안전 처리.
  - 검색 입력 placeholder를 "한국어·영어·메뉴 이름으로 검색..."으로 명확히 표기.
- **사이드바**: `all_translations`를 탭 밖에서 한 번 로드해 탭·사이드바에서 공통 사용.
- **세션**: `selected_tool_translation_data`, `selected_tool_id`, `selected_tool_lang` 초기화 추가.
- **버튼 key**: 탭1/탭2 버튼에 고유 key 부여해 위젯 충돌 방지.

---

## 2. 에러 처리 및 안정성

### 2.1 bare `except` 사용
- **위치**: `pages/1_📊_대시보드.py` (최근 도구/사용자 날짜 파싱), `admin/utils.py` (날짜/객체 변환).
- **문제**: `except:` 또는 `except Exception: pass`로 모든 예외를 삼키면 디버깅이 어려움.
- **권장**: `except (ValueError, TypeError, AttributeError):` 등 구체적 예외만 처리하고, 필요 시 로깅 또는 사용자 메시지 출력.

### 2.2 대시보드 날짜 처리
- **위치**: `pages/1_📊_대시보드.py` – `created_date.replace(tzinfo=None)`.
- **문제**: Firestore `DatetimeWithNanoseconds` 등은 `.replace()`가 없을 수 있음; timezone-aware와 naive 혼용 시 비교 오류 가능.
- **권장**: `admin/utils.py`에 안전한 `to_naive_dt(dt)` 같은 헬퍼를 두고, 대시보드에서 해당 헬퍼 사용.

### 2.3 CRUD 모듈에서 `st.error` 사용
- **위치**: `admin/tools.py`, `admin/users.py`, `admin/translations.py` 등.
- **문제**: 비즈니스 로직 레이어에서 UI 메시지(`st.error`)를 직접 호출하면 테스트·재사용이 어렵고, 페이지 로드 시점에 따라 메시지가 안 나올 수 있음.
- **권장**: 함수는 예외를 던지거나 `(success: bool, message: str)` 형태로 반환하고, 페이지에서 결과에 따라 `st.error`/`st.success` 호출.

### 2.4 applications 컬렉션 fallback
- **위치**: `admin/applications.py` – 여러 경로 시도 후 `tool-registrations` 단일 컬렉션 시도.
- **문제**: `except:`로 모든 예외를 잡아 실제 원인 파악이 어렵고, 컬렉션 구조가 바뀌면 동작이 불명확해짐.
- **권장**: `COLLECTIONS["TOOL_REGISTRATIONS"]` 한 경로만 사용하거나, 설정/환경 변수로 경로를 선택하도록 하고, 실패 시 로그에 예외 타입·메시지 기록.

---

## 3. 세션 상태 및 위젯 key

### 3.1 페이지별 세션 초기화 일관성
- **현황**: 일부 페이지만 `selected_*`, `is_edit_mode` 등을 초기화함.
- **권장**: 그리드 선택·상세·편집을 쓰는 모든 페이지에서 필요한 `st.session_state` 키를 상단에 한곳에서 초기화하고, 주석으로 용도 명시.

### 3.2 AgGrid/폼 위젯 key 충돌
- **위치**: 다국어 관리 탭1·탭2, 사용자/등록 신청 등 여러 그리드·폼이 있는 페이지.
- **권장**: 같은 페이지 내 버튼·입력은 고유 key 사용 (예: `key="i18n_search_btn"`, `key="user_save_btn"`). 이미 다국어 페이지에 적용됨.

### 3.3 사용자 관리 등 다른 그리드 상세
- **권장**: 도구 관리와 동일하게 “다른 행 선택 시 상세가 안 바뀌는” 현상이 있으면, 상세 영역 위젯에 `selected_user_id` 등 기반 동적 key 적용 검토.

---

## 4. 성능 및 캐시

### 4.1 캐시 TTL
- **현황**: `get_all_*` 5분, `get_*_by_id` 1분.
- **권장**: 운영 환경에서는 필요 시 TTL을 환경 변수로 조정 가능하게 하거나, “캐시 초기화” 버튼만으로도 충분한지 검토.

### 4.2 대시보드 데이터 로드
- **현황**: `get_all_tools()`, `get_all_users()`, `get_all_recipes()`, `get_category_statistics()` 등 여러 번 호출.
- **권장**: `get_category_statistics()`가 내부에서 `get_all_tools()`를 부르므로 중복 호출은 캐시로 완화됨. 다만 대시보드 첫 로딩이 느리면 `st.spinner` 메시지를 “도구·사용자·레시피 불러오는 중…” 등으로 구체화.

### 4.3 다국어 페이지 `all_translations` 로드 시점
- **현황**: 탭 상단에서 한 번 로드해 탭1·사이드바에서 사용.
- **권장**: 유지. 탭 전환 시 매번 재로드하지 않아도 됨.

---

## 5. 보안 및 설정

### 5.1 번역/도구 업데이트 시 `updatedBy`
- **위치**: `admin/translations.py`, `admin/paid_services.py` 등 – `updatedBy: "admin"` 하드코딩.
- **권장**: 로그인 사용자 ID가 있다면 세션에서 읽어 사용; 없으면 `"admin"` 또는 환경 변수 `ADMIN_USER_ID` 사용.

### 5.2 Firebase 키 경로
- **위치**: `admin/config.py` – 여러 경로 시도 후 기본값 설정.
- **권장**: 현재 방식 유지. 배포 가이드(DEPLOYMENT.md 등)에 환경 변수 우선 사용을 명시.

### 5.3 설정 페이지에서 키 경로 노출
- **위치**: `pages/9_⚙️_설정.py` – 연결 실패 시 `FIREBASE_SERVICE_ACCOUNT_KEY_PATH` 출력.
- **권장**: 경로만 표시(파일명 제외 전체 경로는 운영에서 제한적으로만 노출)하거나, “설정됨 / 미설정” 정도만 표시.

---

## 6. UI/UX 일관성

### 6.1 언어 선택기
- **현황**: 메인(`admin_main.py`)과 각 페이지에서 `render_language_selector()` 호출. 일부 페이지는 호출하지 않을 수 있음.
- **권장**: 모든 페이지에서 동일하게 `render_language_selector()`를 호출하거나, 레이아웃 컴포넌트로 통합.

### 6.2 render_page_header
- **현황**: 대부분의 페이지에서 `render_page_header` 사용. 메인만 `render_header` 사용.
- **권장**: 네이밍 통일(`render_page_header`로 통일) 또는 용도(메인 vs 서브 페이지)에 따라 이름을 나누고 문서화.

### 6.3 버튼/폼 레이블
- **권장**: “저장”, “취소”, “검색” 등 반복되는 문구는 i18n 키로 빼서 한·영 지원과 일관성 유지.

---

## 7. 데이터 및 스키마

### 7.1 Firestore 타입 변환
- **위치**: `admin/utils.py` – `convert_firestore_data`에서 `datetime`, `DocumentReference` 등 처리.
- **권장**: Firestore의 `DatetimeWithNanoseconds`는 `hasattr(dt, 'isoformat')`로 처리되고 있음. `isoformat()`이 없을 경우를 위해 `getattr(dt, 'isoformat', None)` 호출 후 fallback 유지.

### 7.2 번역 문서 `type` 필드
- **위치**: 다국어 관리 – `TRANSLATION_TYPES` (menu, tool_info, other).
- **권장**: Firestore에 실제로 저장된 `type` 값과 config의 `TRANSLATION_TYPES` 키가 일치하는지 확인. 불일치 시 “기타” 등으로 매핑하도록 코드에 주석 추가.

### 7.3 카테고리 통계
- **위치**: `admin/categories.py` – `get_category_statistics()`에서 `primaryCategory`와 `CATEGORIES` 매핑.
- **문제**: 도구의 `primaryCategory`가 영문/한글 ID 등 다양한 형식일 수 있어, 매핑이 누락될 수 있음.
- **권장**: `primaryCategory` 값 목록을 로그로 한 번 확인하고, `CATEGORIES`의 `id`/`name`과 대응 관계를 명확히 하거나, 도구 스키마 문서에 “primaryCategory는 config의 어떤 키와 매핑되는지” 정리.

---

## 8. 테스트 및 유지보수

### 8.1 단위 테스트
- **현황**: 테스트 디렉터리/파일 없음.
- **권장**: `admin/utils.py`의 `normalize_id`, `convert_firestore_data`, `format_value` 등부터 pytest로 단위 테스트 추가. Firebase 의존 함수는 mock으로 처리.

### 8.2 타입 힌트
- **현황**: 대부분의 함수에 `List`, `Dict`, `Optional` 등 타입 힌트 있음.
- **권장**: 반환 타입이 복잡한 경우 `TypedDict` 또는 프로토콜로 “도구/사용자/번역 문서” 형태를 정의하면 리팩터링 시 유리.

### 8.3 로깅
- **현황**: `st.error`/`st.warning` 위주.
- **권장**: 운영 환경에서는 `logging` 모듈로 예외·실패 원인을 로그에 남기고, 사용자에는 간단한 메시지만 표시.

---

## 9. 문서 및 배포

### 9.1 README / QUICK_DEPLOY
- **권장**: 환경 변수 목록, Firebase 컬렉션 구조 요약, “캐시 초기화” 사용 시점을 QUICK_DEPLOY.md에 추가.

### 9.2 requirements.txt
- **권장**: `streamlit`, `firebase-admin`, `st-aggrid`, `pandas`, `plotly` 등 버전 고정(예: `streamlit>=1.28,<2`)하여 재현 가능한 배포 유지.

---

## 10. 요약 체크리스트

| 구분 | 항목 | 우선순위 |
|------|------|----------|
| 안정성 | bare except 제거, 예외 구체화 | 높음 |
| 안정성 | 대시보드 날짜 처리 헬퍼 도입 | 중간 |
| 아키텍처 | CRUD에서 st.error 제거, 반환값으로 처리 | 중간 |
| 세션/key | 사용자·등록 신청 등 상세 영역 동적 key 검토 | 중간 |
| 보안 | updatedBy를 세션/환경 변수로 | 낮음 |
| 일관성 | 언어 선택기·헤더 컴포넌트 통일 | 낮음 |
| 테스트 | utils 등 핵심 로직 단위 테스트 | 낮음 |
| 문서 | 환경 변수·컬렉션 구조 문서화 | 낮음 |

위 항목을 단계적으로 반영하면 어드민 전반의 안정성과 유지보수성이 개선됩니다.
